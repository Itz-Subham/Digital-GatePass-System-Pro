<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.gatepass"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-feature android:name="android.hardware.camera.any" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" android:maxSdkVersion="28" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.GatePass"
        tools:targetApi="31">

        <activity
            android:name=".activities.LoginActivity"
            android:exported="true"
            android:theme="@style/Theme.GatePass">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".activities.DashboardActivity"
            android:exported="false"
            android:theme="@style/Theme.GatePass" />

        <activity
            android:name=".activities.ManageGuardsActivity"
            android:exported="false"
            android:theme="@style/Theme.GatePass" />

        <activity
            android:name=".activities.ChangePasswordActivity"
            android:exported="false"
            android:theme="@style/Theme.GatePass" />

        <activity
            android:name=".activities.MainActivity"
            android:exported="false" />

        <activity
            android:name=".activities.GatePassFormActivity"
            android:exported="false" />

        <activity
            android:name=".activities.GatePassPreviewActivity"
            android:exported="false" />

        <activity
            android:name=".activities.ActiveVisitorsActivity"
            android:exported="false" />

        <activity
            android:name=".activities.HistoryActivity"
            android:exported="false" />

        <activity
            android:name=".activities.SecurityListActivity"
            android:exported="false" />

        <activity
            android:name=".activities.CheckoutScannerActivity"
            android:exported="false" />

        <activity
            android:name=".activities.CheckoutDetailActivity"
            android:exported="false" />

        <!-- Removed HallpassSimulationActivity -->

        <activity
            android:name=".activities.ManualSelectionActivity"
            android:exported="false" />

        <activity
            android:name=".activities.DisplayQrActivity"
            android:exported="false" />

    </application>

</manifest>